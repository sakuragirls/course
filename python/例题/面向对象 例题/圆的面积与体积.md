```
class rounds:
    def __init__(self, radius):
        self.radius = radius

def area(self):
    bj = 2 * 3.14 * self.radius
    print('圆的面积为{}'.format(bj))

s1 = rounds(2)  # 对rounds类实例化
s1.area()       # 调用rounds中的area函数
```
